#pragma once
#include <vector>
#include <string>
using namespace std;

extern vector<int> vec;

int sum();
vector<int> read();
void write(vector<int> n);
void print(const string& text);
void display();
vector<int> input();
vector<int> load();
void save();